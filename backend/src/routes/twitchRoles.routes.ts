import { FastifyPluginAsync } from 'fastify'; import { Platform } from '@prisma/client'; import { z } from 'zod';
import { prisma } from '../db/prisma.js'; import { AuthedRequest, requireAuth, requireChannelRole } from '../auth/guards.js'; import { TwitchApi, TwitchApiError } from '../twitch/TwitchApi.js'; import { requireBroadcasterScopes } from '../services/twitchTokenService.js';
const twitchApi = new TwitchApi(); const roleActionSchema=z.object({action:z.enum(['make_moderator','remove_moderator','make_vip','remove_vip']),username:z.string().max(100).optional()}).strict(); const requiredRoleScopes=['channel:manage:moderators','channel:read:vips','channel:manage:vips'];
const twitchRolesRoutes: FastifyPluginAsync=async(app)=>{ app.post('/api/channels/:channelId/twitch/chatters/:userId/role',{preHandler:requireAuth},async(req:any,rep)=>{await requireChannelRole(req as AuthedRequest,rep,'channel_admin'); const body=roleActionSchema.safeParse(req.body); if(!body.success) return rep.code(400).send({errorCode:'validation.failed',details:body.error.issues}); const {channelId,userId}=req.params as any; const channel=await prisma.channel.findUnique({where:{id:channelId}}); if(!channel) return rep.code(404).send({errorCode:'channel.not_found'}); if(userId===channel.twitchChannelId) return rep.code(409).send({errorCode:'twitch.roles.cannot_modify_broadcaster'}); const scoped=await requireBroadcasterScopes(channelId,requiredRoleScopes); if(!scoped.token) return rep.code(400).send({errorCode:'twitch.roles.auth_required'}); if(scoped.missingScopes.length) return rep.code(400).send({errorCode:'twitch.roles.scope_missing',missingScopes:scoped.missingScopes}); const t=scoped.token; try{const isMod=(await twitchApi.getChannelModerators({broadcasterId:channel.twitchChannelId,userId,accessToken:t.accessToken,first:1})).data.length>0; const isVip=(await twitchApi.getChannelVips({broadcasterId:channel.twitchChannelId,userId,accessToken:t.accessToken,first:1})).data.length>0; const action=body.data.action; if(action==='make_moderator'&&isMod) return rep.code(409).send({errorCode:'twitch.roles.already_moderator'}); if(action==='remove_moderator'&&!isMod) return rep.code(409).send({errorCode:'twitch.roles.not_moderator'}); if(action==='make_vip'&&isVip) return rep.code(409).send({errorCode:'twitch.roles.already_vip'}); if(action==='remove_vip'&&!isVip) return rep.code(409).send({errorCode:'twitch.roles.not_vip'}); if(action==='make_moderator') await twitchApi.addChannelModerator({broadcasterId:channel.twitchChannelId,userId,accessToken:t.accessToken}); if(action==='remove_moderator') await twitchApi.removeChannelModerator({broadcasterId:channel.twitchChannelId,userId,accessToken:t.accessToken}); if(action==='make_vip') await twitchApi.addChannelVip({broadcasterId:channel.twitchChannelId,userId,accessToken:t.accessToken}); if(action==='remove_vip') await twitchApi.removeChannelVip({broadcasterId:channel.twitchChannelId,userId,accessToken:t.accessToken}); const roleType=action.includes('moderator')?'moderator':'vip'; const actionType=action.startsWith('make')?'add':'remove'; await prisma.twitchRoleAction.create({data:{channelId,targetExternalUserId:userId,targetUsername:body.data.username,roleType,actionType,createdByUserId:req.session.userId}}); await prisma.auditLog.create({data:{channelId,userId:req.session.userId,action:`twitch.role.${action}`,detailsJson:JSON.stringify({targetExternalUserId:userId,targetUsername:body.data.username,roleType,actionType})}}); await prisma.botEvent.create({data:{channelId,platform:Platform.twitch,eventType:'twitch.role.changed',payloadJson:JSON.stringify({action,targetExternalUserId:userId,targetUsername:body.data.username,roleType,actionType})}}); return {ok:true,action,userId,username:body.data.username??null,roleAfter:actionType==='add'?roleType:'viewer'};}catch(e){if(e instanceof TwitchApiError) return rep.code(502).send({errorCode:'twitch.roles.api_failed',status:e.status,safeMessage:e.safeMessage}); return rep.code(502).send({errorCode:'twitch.roles.api_failed'});} });
app.get('/api/channels/:channelId/twitch/role-actions',{preHandler:requireAuth},async(req,rep)=>{await requireChannelRole(req as AuthedRequest,rep,'channel_moderator'); const {channelId}=req.params as any; const q=req.query as any; const limit=Math.min(Math.max(Number(q.limit||100),1),500); const items=await prisma.twitchRoleAction.findMany({where:{channelId,...(q.roleType?{roleType:q.roleType}:{}),...(q.actionType?{actionType:q.actionType}:{}),...(q.username?{targetUsername:{contains:q.username,mode:'insensitive'}}:{})},orderBy:{createdAt:'desc'},take:limit}); return {items};});};
export default twitchRolesRoutes;
